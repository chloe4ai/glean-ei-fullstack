{
  "name": "glean-enterprise-intelligence",
  "version": "1.0.0",
  "private": true,
  "description": "Glean Enterprise Intelligence — full-stack interview prototype (React + Express)",
  "scripts": {
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "start": "npm --prefix backend install --omit=dev && node backend/server.js",
    "start:backend": "npm --prefix backend start",
    "start:frontend": "npm --prefix frontend run dev"
  },
  "engines": { "node": ">=18" },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
